[project]

name = "jinja-templating"
requires-python = ">=3.11"
version = "1.0"

dependencies = [
    "jinja2",
    "pyyaml",
]

[project.optional-dependencies]
dev = ["pylint", "pytest"]

[project.scripts]
jte = "jte:main_cli"

[tool.pytest.ini_options]
testpaths = [
    "unit_tests",
    "tests",
]

log_cli = "True"
log_cli_level = "DEBUG"